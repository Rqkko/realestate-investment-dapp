
module.exports = async function(deployer) {
  // TODO: Make initial test data

  // let factory = await ProjectFactory.deployed()
  // await factory.createProject(web3.utils.toWei("5", "ether"), { from: accounts[0] })
  // let addresses = await factory.getAllProjects()
  // let project = await Project.at(addresses[0])
  // await project.invest({ from: accounts[1], value: web3.utils.toWei("1", "ether") })
}